(function ()
{
    var textbox = window.objects.textbox = function (text, choices)
    {
        var placed = "";
        var to_place = text;
        var end_of_box = false;
        var text_objs = [];
        var box = null;
    };

    textbox.prototype.draw = function (ctx) {
        strokeRect();
    };

})();
